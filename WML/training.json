{

    "action": "${action}",
    "outputType": "DATA_URL",
    "model": {
        "id": "${modelId}",
		    "name": "${modelName}"
    },
    "dbDefinitions": {
         "db1":
         {
                   "type": "DashDB",
                   "db": "BLUDB",
                   "host": "${db1Host}",
                   "options": "",
                   "password": "${db1Password}",
                   "port": "${db2Port}",
                   "username": "${db2Username}"
                   }
    },
    
    "setting": {
        "inputs": [
        {
        "node": "${inputsNode1}",
                "odbc": {
                    "dbRef": "db2",
                    "table": "${tableName1}"
                }
            },
        {
        "node": "${inputsNode2}",
                "odbc": {
                    "dbRef": "db2",
                    "table": "${tableName2}"
                }
            }
        
        ],
        "exports":[
        {
        "odbc":{
        "dbRef":"db2",
        "table":"SPSS_MODEL_OUTPUT_TABLE",
        "insertMode": "Refresh"
   },
    "node": "SPSS_MODEL_OUTPUT_TABLE",
    "attributes": []
  }
  ],
 
       "reportFormat": "TAB"
    }
}

